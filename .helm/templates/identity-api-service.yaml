apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.identityApi.service.name }}
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.identityApi.service.name }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.identityApi.service.portHttp }}
      targetPort: {{ .Values.identityApi.service.portHttp }}
