{{- if .Values.assessmentApi.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Values.assessmentApi.service.name }}-ingress
spec:
  rules:
    - host: {{ .Values.assessmentApi.ingress.host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Values.assessmentApi.service.name }}
                port:
                  number: {{ .Values.assessmentApi.service.portHttp }}
{{- end }}