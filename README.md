# StudentTestingSystem

Платформа для автоматизированного тестирования студентов с AI.

## 🚀 Быстрый старт

1. **Создай секреты** (см. [.helm/SECRETS.md](.helm/SECRETS.md))
2. **Настрой values.yaml** под свою инфраструктуру
3. **Задеплой:**
   ```bash
   werf converge --repo registry.lmscloud.ru/student-testing-system
   ```

## 📁 Структура

```
src/
├── backend/          # .NET микросервисы
│   ├── Assessment    # API тестов и заданий
│   ├── Media         # API работы с файлами (S3)
│   ├── Identity      # API пользователей и групп
│   └── Grading       # API AI-проверки (Gemini)
└── frontend/         # React SPA

.helm/                # Kubernetes манифесты
├── templates/        # Deployments, Services, Ingress
├── values.yaml       # Конфигурация (не в git)
└── SECRETS.md        # Инструкции по секретам

docker/               # Dockerfiles для backend
werf.yaml            # Конфигурация werf
```

## 🔧 Конфигурация

Все настройки в `.helm/values.yaml`:
- Домены для сервисов
- Resource limits
- URL внешних сервисов (Keycloak, MinIO, RabbitMQ)

Секреты в Kubernetes Secrets:
- Пароли БД, Redis, RabbitMQ
- API ключи (Gemini)
- S3 credentials

## 🌐 Архитектура

```
Frontend (lmscloud.ru)
    ↓
Traefik Ingress
    ↓
┌─────────────┬──────────────┬──────────────┬──────────────┐
│ Assessment  │    Media     │   Identity   │   Grading    │
│     API     │     API      │     API      │     API      │
└──────┬──────┴──────┬───────┴──────┬───────┴──────┬───────┘
       │             │              │              │
       ├─────────────┴──────────────┘              │
       │        PostgreSQL                         │
       │                                           │
       ├───────────────────────────────────────────┤
       │              RabbitMQ (async grading)     │
       └───────────────────────────────────────────┘
```

## 📚 Технологии

- **Backend:** .NET 8, Clean Architecture, CQRS, MediatR
- **Frontend:** React, Keycloak Auth, Runtime Config
- **AI:** Google Gemini API (через Cloudflare Workers proxy)
- **Storage:** MinIO S3, PostgreSQL, Redis
- **Deployment:** Kubernetes, Helm, Werf
